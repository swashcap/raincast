const reducer = (
  state = {
    data: {},
    error: null,
    isLoading: false,
    lastFetched: null
  },
  { action, payload }
) => {
  switch (action) {
    default:
      return state
  }
}

module.exports = reducer
